IF you want to directly manipulate the db tables in `db.sqlite`, tehn do the following.

```bash
python manage.py dbshell
# then type in sqlite3 db commands: 
# e.g. .table
# e.g. CREATE TABLE xxxxxxx
```
